//list of all users 

<%- include('../partials/header.ejs') %>

  <%- include('../journals/nav.ejs') %>

  <h3>All entries for user: <%= user.username %></h3>


<ul>
    <% journals.forEach((journal) => { %>
      <li>
        <a href="/journals/<%= journal.id %>"><%= journal.name %></a>
      </li>
    <% }) %>    
  </ul>

  <strong>sort by date</strong> 
  //make into a button 

<%- include('../partials/footer.ejs') %>